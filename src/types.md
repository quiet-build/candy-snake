# types.ts

## Purpose

Documents src/types.ts, a domain model/types in this project. It defines Direction, FoodKind, PowerUpKind, Cell, OPPOSITE, DIR_VECTORS.

## Behavior

Shapes shared domain data. Keep migrations, serializers, API contracts, and fixtures synchronized when these fields change.

## Maintenance

Update this companion when src/types.ts changes in a way that affects public behavior, configuration, dependencies, data shape, or maintenance expectations.
