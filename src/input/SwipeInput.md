# SwipeInput.ts

## Purpose

Documents src/input/SwipeInput.ts, a source/config file in this project. It defines SwipeInput.

## Behavior

Provides maintained project behavior. Keep callers, tests, and configuration references synchronized when editing it.

## Maintenance

Update this companion when src/input/SwipeInput.ts changes in a way that affects public behavior, configuration, dependencies, data shape, or maintenance expectations.
