# BootScene.ts

Loads the eight existing audio assets relative to the built game module's origin (`../audio/` from `assets/`), so embedding does not request audio from the arcade host. Starts MenuScene after loading. Keep the build chunk directory and this URL aligned.
