# config.ts

## Purpose

Documents src/config.ts, a source/config file in this project. It defines CONFIG.

## Behavior

Provides maintained project behavior. Keep callers, tests, and configuration references synchronized when editing it.

## Maintenance

Update this companion when src/config.ts changes in a way that affects public behavior, configuration, dependencies, data shape, or maintenance expectations.
