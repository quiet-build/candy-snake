# LevelBanner.ts

## Purpose

Documents src/ui/LevelBanner.ts, a UI surface in this project. It defines showLevelBanner.

## Behavior

Defines user-facing interface behavior. Keep props, state transitions, accessibility, and styling expectations synchronized with its consumers.

## Maintenance

Update this companion when src/ui/LevelBanner.ts changes in a way that affects public behavior, configuration, dependencies, data shape, or maintenance expectations.
