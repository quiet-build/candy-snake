# ScorePopup.ts

## Purpose

Documents src/ui/ScorePopup.ts, a UI surface in this project. It defines showScorePopup.

## Behavior

Defines user-facing interface behavior. Keep props, state transitions, accessibility, and styling expectations synchronized with its consumers.

## Maintenance

Update this companion when src/ui/ScorePopup.ts changes in a way that affects public behavior, configuration, dependencies, data shape, or maintenance expectations.
